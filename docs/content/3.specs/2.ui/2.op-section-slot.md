---
title: 'Task 2.2: OpSection & OpSlot'
description: Basis-Komponenten fuer das Composition System.
---

# Task 2.2: OpSection & OpSlot — Basis-Komponenten

**EPIC:** 2 — UI Engine & Component Bridge (Op-System)
**Paket:** `@openpress/ui`
**Pfad:** `packages/ui/src/runtime/components/`
**Abhaengigkeiten:** Task 2.1 (OpProvider)

---

## 1. Zielsetzung

`OpSection` und `OpSlot` bilden das Composition-System von OpenPress. Jede Seite besteht aus Sections, die Slots enthalten, die wiederum Blocks aufnehmen. Diese Komponenten sind die strukturellen Bausteine — sie definieren **Layout**, nicht **Inhalt**.

```text
Page
 +-- OpSection (type: "hero")
 |    |-- OpSlot (name: "default")
 |    |    |-- OpBlock (type: "rich-text")
 |    |    +-- OpBlock (type: "button")
 |    +-- OpSlot (name: "media")
 |         +-- OpBlock (type: "image")
 +-- OpSection (type: "features")
      +-- OpSlot (name: "default")
           |-- OpBlock (type: "card")
           |-- OpBlock (type: "card")
           +-- OpBlock (type: "card")
```

## 2. Schema-Referenz

```typescript
SectionSchema = z.object({
  id: z.string().ulid(),
  type: z.string().min(1),
  slots: z.record(SlotSchema),
})

SlotSchema = z.array(BlockSchema)

BlockSchema = z.object({
  id: z.string().ulid(),
  type: z.string().min(1),
  props: z.record(z.unknown()),
})
```

## 3. OpSection

### Props

| Prop | Typ | Required | Default | Beschreibung |
|------|-----|----------|---------|-------------|
| `section` | `Section` | ja | — | Section-Daten aus dem Page-Schema |
| `ui` | `OpSectionUI` | nein | `{}` | Klassen-Overrides (Nuxt UI Pattern) |

### Rendering-Strategie

OpSection hat **zwei Modi**:

1. **Auto-Rendering (Default):** Iteriert ueber `section.slots` und rendert fuer jeden einen `<OpSlot>`.

2. **Manuelles Rendering (Scoped Slot):** Themes koennen den Default-Slot ueberschreiben:

```vue
<OpSection :section="section">
  <template #default="{ slots }">
    <div class="grid grid-cols-2 gap-8">
      <OpSlot name="default" :blocks="slots.default" />
      <OpSlot name="media" :blocks="slots.media" />
    </div>
  </template>
</OpSection>
```

### Data-Attribute

| Attribut | Wert | Zweck |
|----------|------|-------|
| `data-op-section` | Section-Type | Theme-Scoping via CSS |
| `data-op-id` | ULID | Identifikation fuer Editor-Overlay |
| `data-op-editing` | (present/absent) | Editor-spezifische Styles |

## 4. OpSlot

### Props

| Prop | Typ | Required | Default | Beschreibung |
|------|-----|----------|---------|-------------|
| `name` | `string` | ja | — | Slot-Name (z.B. `"default"`, `"sidebar"`) |
| `blocks` | `Block[]` | ja | — | Geordnete Block-Liste |
| `ui` | `OpSlotUI` | nein | `{}` | Klassen-Overrides |

### Verhalten

- Rendert Blocks in der richtigen Reihenfolge
- Leerer Slot im Edit-Modus zeigt Platzhalter
- Leerer Slot im View-Modus rendert nichts extra

## 5. Block-Resolution

### Block-Registry

```typescript
const blockRegistry = new Map<string, Component>()

export function registerBlock(type: string, component: Component) {
  blockRegistry.set(type, component)
}

export function resolveBlockComponent(type: string): Component {
  return blockRegistry.get(type) ?? OpBlockFallback
}
```

### OpBlockFallback

Rendert im View-Modus nichts. Im Edit-Modus zeigt es den Block-Type als Warnung an:

```vue
<template>
  <div v-if="isEditing" class="op-block-fallback" data-op-fallback>
    Unbekannter Block-Typ: {{ block.type }}
  </div>
</template>
```

## 6. Provide/Inject Hierarchie

```text
OpProvider
|  provides: OP_PAGE_KEY, OP_SITE_KEY, OP_NAV_KEY, OP_MODE_KEY, OP_THEME_KEY
|
+-- OpSection
    |  injects:  OP_MODE_KEY, OP_THEME_KEY
    |  provides: OP_SECTION_KEY
    |
    +-- OpSlot
        |  injects:  OP_SECTION_KEY, OP_MODE_KEY, OP_THEME_KEY
        |  provides: OP_SLOT_KEY
        |
        +-- OpBlock (resolved component)
             injects: OP_SLOT_KEY, OP_SECTION_KEY, OP_MODE_KEY
```

## 7. Edit-Modus Verhalten

OpSection und OpSlot liefern nur die `data-*` Attribute und den Kontext. Die tatsaechliche Editor-Interaktion (Overlays, Drag & Drop, Toolbar) wird in EPIC 3 implementiert. Die Basis-Komponenten sind absichtlich edit-mode-**aware** aber nicht edit-mode-**implementing**.

### OpSection im Edit-Modus

| Feature | Beschreibung |
|---------|-------------|
| Hover-Outline | Blaue Outline beim Hover |
| Selection | Click selektiert die Section (EPIC 3) |
| Drag-Handle | Handle zum Umsortieren (EPIC 3) |
| Add-Button | "Section hinzufuegen" zwischen Sections (EPIC 3) |

### OpSlot im Edit-Modus

| Feature | Beschreibung |
|---------|-------------|
| Drop-Zone | Akzeptiert Blocks via Drag & Drop |
| Empty-State | Zeigt Platzhalter wenn leer |
| Add-Trigger | UI-Hook fuer "Block hinzufuegen" |

## 8. Nutzungsbeispiel

### Minimal (Auto-Rendering)

```vue
<template>
  <OpProvider :page="page" :site="site">
    <OpSection
      v-for="section in page.sections"
      :key="section.id"
      :section="section"
    />
  </OpProvider>
</template>
```

### Block-Registrierung

```typescript
import { registerBlock } from '@openpress/ui'
import OpRichText from './OpRichText.vue'
import OpImage from './OpImage.vue'

registerBlock('rich-text', OpRichText)
registerBlock('image', OpImage)
```

## 9. Testplan

| Test | Beschreibung |
|------|-------------|
| Section rendert Slots | Section mit 2 Slots -> 2 OpSlot Kinder |
| Section provide | `useOpSection()` in Kind gibt Section zurueck |
| Section data-Attribute | `data-op-section`, `data-op-id` vorhanden |
| Slot rendert Blocks | Slot mit 3 Blocks -> 3 Block-Komponenten |
| Slot Empty State | Leerer Slot im Edit-Modus zeigt Platzhalter |
| Block Fallback | Unbekannter Block-Type -> OpBlockFallback |
| Block Registry | `registerBlock()` + `resolveBlockComponent()` |
| Scoped Slot Override | Theme kann Section-Layout ueberschreiben |
| Inject Hierarchie | Section -> Slot -> Block: Korrekte Kontexte |

## 10. Akzeptanzkriterien

- OpSection rendert semantisches `<section>` Element mit data-Attributen
- OpSection stellt Section-Kontext via provide/inject bereit
- OpSlot rendert Blocks in der richtigen Reihenfolge
- OpSlot zeigt Empty-State nur im Edit-Modus
- Block-Registry loest Types auf registrierte Komponenten auf
- OpBlockFallback fuer unbekannte Block-Types
- Scoped-Slot Pattern ermoeglicht Theme-spezifische Layouts
- `useOpSection()` und `useOpSlot()` Composables funktionieren
- `ui` Prop fuer Klassen-Overrides (Nuxt UI Pattern)
