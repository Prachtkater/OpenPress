---
title: Pakete
description: Ueberblick ueber alle Pakete im OpenPress-Monorepo.
---

# Pakete

## Core-Pakete

### `@openpress/schemas`

Zentrale Zod-Schema-Definitionen fuer OpenPress.

- **Single Source of Truth** fuer alle Datenstrukturen
- Page-Schema: Seitenstruktur mit Sections, Slots und Blocks
- Block-Schemas: Typdefinitionen fuer jeden Block-Typ
- Navigation-Schema: Menustruktur und Links
- Site-Config-Schema: Globale Website-Einstellungen
- Feature-Manifest-Schema: Definition fuer Feature-Module
- TypeScript-Types werden automatisch aus Zod-Schemas abgeleitet

```
src/
  blocks/         # Block-spezifische Schemas (rich-text, image, ...)
  page.ts         # Page-Struktur Schema
  navigation.ts   # Navigations-Schema
  site.ts         # Site-Config Schema
  manifest.ts     # Feature-Manifest Schema
  index.ts        # Re-exports
```

### `@openpress/core`

Das Nuxt Module — Einstiegspunkt fuer OpenPress.

- Registriert das `/_edit` Routing fuer den Visual Editor
- Injiziert Runtime-Komponenten, Composables und Server-Utilities
- Stellt die Konfigurationsbruecke zwischen Nuxt und OpenPress her
- HMR-Integration fuer JSON-Inhalte
- Git-Storage Bridge als Server-Middleware

### `@openpress/ui`

Glow-Frame Editor UI Components & Theme Engine.

- `OpProvider`: Root-Komponente, Global State via provide/inject
- `OpSection`: Section-Renderer mit Theme-Klassen
- `OpSlot`: Slot-Renderer mit Block-Resolution
- `OpBlockFallback`: Fallback fuer unbekannte Block-Typen
- 5-Layer Class Resolution: Theme → Variants → Compound → Config → UI Prop

## Themes

### `@openpress/theme-tailwind-plus`

Theme basierend auf Tailwind CSS. Liefert Klassen-Mappings fuer alle Core-Bloecke im Tailwind-Stil.

### `@openpress/theme-material-expressive`

Theme basierend auf Google Material Design 3 — Expressive.

- Material Design 3 "Expressive" Styling fuer alle Core-Bloecke
- Dynamic Color: Theme-Generierung aus Primaerfarbe
- Material Motion: Smooth Transitions und Animationen
- Responsive Design mit Material Breakpoints
- Component-Tokens fuer granulare Anpassung
- Section-Templates im Material-Stil

## Features

### `@openpress/feature-contact-form`

Kontaktformular-Feature mit Feature-Manifest-Integration.

### `@openpress/feature-booking`

Buchungssystem-Feature (geplant).

## POCs

### `@openpress/poc-storage`

POC 1: Git-backed JSON Storage Engine. Validiert die technische Machbarkeit fuer:

- **Read**: JSON-Dateien aus dem Git-Repository lesen und parsen
- **Write**: Strukturierte JSON-Daten ins Dateisystem schreiben
- **Commit**: Aenderungen automatisch als Git-Commits speichern
- **Zod-Validierung**: Jede Read/Write-Operation gegen Zod-Schemas validieren
- **ULID-basierte IDs**: Persistente, sortierbare Block-Identifikatoren
- **Conflict Detection**: Erkennung konkurrierender Aenderungen

Status: 13/13 Tests bestanden.

### `@openpress/poc-editor`

POC 2: The Glow Frame — In-Context Overlay UI Prototyp.

- **Glow-Frame Rendering**: Leuchtender Rahmen um hoverte/selektierte Bloecke
- **Inline-Editing**: Tiptap-basiertes Rich-Text-Editing direkt im Content
- **Block-Toolbar**: Kontextuelle Aktionen (Verschieben, Loeschen, Einstellungen)
- **Drag & Drop**: Block-Reordering innerhalb von Slots
- **Vue-Komponenten-Mapping**: Zod-Schema zu Vue-Property-Panel Mapping

Status: Shell-Paket, Implementierung ausstehend.

## Playground

Entwicklungs- und Test-Umgebung. Referenziert alle Workspace-Pakete und dient als Basis fuer die lokale Entwicklung.
